/**
 * These are the extension-resources passed in by Razor to JS, for use in the UI.
 * They are defined in the UI and each installation can override them as needed.
 */
export interface Resources {
  SearchLabel: string;
  ViewConfigModeLabel: string;
}